{
  "tasks": {
    "build": "npm install && npm run build",
    "start": "deno run --allow-net --allow-read --allow-env deno-server.ts"
  },
  "deploy": {
    "entrypoint": "deno-server.ts",
    "include": ["dist", "deno-server.ts"]
  }
}
